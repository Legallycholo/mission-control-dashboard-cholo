CREATE TABLE IF NOT EXISTS `YOUR_GCP_PROJECT_ID.raw_layer.market_intelligence_launches` (
  id STRING,
  producto STRING,
  marca STRING,
  categoria STRING,
  especificaciones_clave STRING,
  fuente STRING,
  estado_db STRING,
  fecha_escaneo TIMESTAMP
);
